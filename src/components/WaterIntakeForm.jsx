import React, { useState } from 'react';\n\nconst WaterIntakeForm = () => {\n    const [intake, setIntake] = useState('');\n    const [showModal, setShowModal] = useState(false);\n\n    const handleInputChange = (e) => {\n        setIntake(e.target.value);\n    };\n\n    const handleSubmit = (e) => {\n        e.preventDefault();\n        // Add logic to handle water intake submission.\n        console.log(`Water intake submitted: ${intake} liters`);\n        setIntake('');\n        setShowModal(false);\n    };\n\n    return (\n        <div>\n            <button onClick={() => setShowModal(true)}>Log Water Intake</button>\n\n            {showModal && (\n                <div className="modal">\n                    <form onSubmit={handleSubmit}>\n                        <label htmlFor="intake">Water Intake (liters):</label>\n                        <input\n                            type="number"\n                            id="intake"\n                            value={intake} \n                            onChange={handleInputChange} \n                            required\n                        />\n                        <button type="submit">Submit</button>\n                        <button type="button" onClick={() => setShowModal(false)}>Cancel</button>\n                    </form>\n                </div>\n            )} \n        </div>\n    );\n};\n\nexport default WaterIntakeForm;